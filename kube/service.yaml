apiVersion: v1
kind: Service
metadata:
  name: league-of-locals-admin-panel-svc
spec:
  type: NodePort
  selector:
    app: league-of-locals-admin-panel
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
